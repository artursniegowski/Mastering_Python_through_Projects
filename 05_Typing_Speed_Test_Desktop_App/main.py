# the main file
from ui import UI

if __name__ == '__main__':
    # creating GUI object
    GUI_typing_speed_test = UI()
    # waiting on the user to close the window
    GUI_typing_speed_test.mainloop()